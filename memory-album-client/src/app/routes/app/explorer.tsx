import ModalPanel from '@/components/ui/modal-panel';

export default function ExplorerRoute() {
  return (
    <ModalPanel
      sx={{
        position: 'absolute',
        top: 0,
      }}
    >
      Test
    </ModalPanel>
  );
}
